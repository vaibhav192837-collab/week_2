[project]
name = "week-2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.133.0",
    "uvicorn>=0.41.0",
]

[tool.uv.workspace]
members = [
    "task_project",
]
